# installed in ~/personal/ml/.venv (uv, CPU-only). Listed, not pinned to a lockfile,
# because the venv is shared across every block of the ML track.
torch==2.14.0+cpu
torchvision==0.29.0+cpu
mujoco==3.12.0
opencv-python-headless==5.0.0
onnx
onnxruntime==1.23.2
matplotlib
tqdm
