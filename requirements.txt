# CPU-only builds throughout -- there is no CUDA anywhere in this project.
# Versions are the ones every number in the README was measured on.
torch==2.14.0+cpu
torchvision==0.29.0+cpu
mujoco==3.12.0
opencv-python-headless==5.0.0
onnx
onnxruntime==1.23.2
matplotlib
tqdm
